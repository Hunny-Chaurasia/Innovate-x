import { AuthPage } from '../../integration/auth';
export default function FacultyRegister() { return <><p className="p-4 text-sm text-amber-300 bg-[var(--background)]">Faculty and industry accounts must be provisioned by an administrator. Sign in with your existing account.</p><AuthPage /></>; }
